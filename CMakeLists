cmake_minimum_required(VERSION 3.10)
project(avhud)

add_executable(avhud src/main.cpp)

target_sources(avhud
    PRIVATE
    src/core/state.h
)

target_include_directories(avhud
    PRIVATE
    src
)
